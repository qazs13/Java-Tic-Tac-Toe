• There is a private variable to represengt the game board on the console:
	- char [][] grid
• There are 2 functions that intracts with it:
	- void printGrid():
		--> to print the grid on the console
	- void regMove(int position, char symbol)
		--> to update the grid with played move

• To integrate with the GUI:
	- replace the console grid with the GUI grid
	- instead of printing the whole grid, update the GUI grid with new moves
	- change the functionality of regMove function or overload it to update 
	  the GUI grid